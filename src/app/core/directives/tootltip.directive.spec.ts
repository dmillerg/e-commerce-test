import { TootltipDirective } from './tootltip.directive';

describe('TootltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TootltipDirective();
    expect(directive).toBeTruthy();
  });
});
