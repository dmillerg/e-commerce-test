import { ErrorHightlightDirective } from './error-hightlight.directive';

describe('ErrorHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
