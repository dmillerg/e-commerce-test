import { MontYearFormatterDirective } from './mont-year-formatter.directive';

describe('MontYearFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new MontYearFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
