import { FallbackImagesTsDirective } from './fallback-images.ts.directive';

describe('FallbackImagesTsDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackImagesTsDirective();
    expect(directive).toBeTruthy();
  });
});
