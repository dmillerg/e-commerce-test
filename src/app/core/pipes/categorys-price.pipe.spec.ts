import { CategorysPricePipe } from './categorys-price.pipe';

describe('CategorysPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CategorysPricePipe();
    expect(pipe).toBeTruthy();
  });
});
